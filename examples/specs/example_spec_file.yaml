# example_spec_file.yaml
# A sample specification file to generate a SingleModeWaveguide project

# Output Directory Locations
project_dir: ~/Photonics_Data/Generated_Waveguide  # Top level directory for the project. This is an absolute path

# This is an optional parameter that can be used to override the layermap used. By default, BPG looks for the
# BAG_PHOT_LAYERMAP environmental variable set in the .bashrc
#layermap: BPG/examples/tech/gds_map.yaml

# Output Settings
lsf_filename: SingleModeWaveguide.lsf
gds_filename: Output

# Generator Params
layout_package: 'LumericalWaveguide'  # Module that contains the layout generator class
layout_class: 'SingleModeWaveguide'  # Layout generator class name
layout_params:  # Place parameters to be passed to the generator class under here

# Cadence related parameters
impl_lib: 'waveguide_lib'
impl_cell: 'waveguide_cell'

# Vestigial parameters required by BAG class
sweep_params:
  intent: [standard]
root_dir: 'data'
